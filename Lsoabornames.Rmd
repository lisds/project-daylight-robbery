---
jupyter:
  jupytext:
    text_representation:
      extension: .Rmd
      format_name: rmarkdown
      format_version: '1.2'
      jupytext_version: 1.15.2
  kernelspec:
    display_name: Python 3 (ipykernel)
    language: python
    name: python3
---

```{python}
import pandas as pd
```

fix lsoa borough names

```{python}
df = pd.read_excel(r"LAD_DEC_2021_UK_NC.xlsx")
```

```{python}
# stuff = list(data.lsoa.index.get_level_values(2).unique().sort_values())
# df1 = data.lsoa

# df.columns = pd.MultiIndex.from_product([['a'], df.columns])

# merged = pd.merge(data.lsoa, df, on='borough')
data.lsoa.merge(df, left_index=True, right_on=['borough'])
```
